name: Build SafarGo Customer APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter Engine
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.22.0'
          channel: 'stable'

      - name: Create Clean Android Structure
        run: flutter create . --platforms=android --org com.safargo.customer --project-name safargo_customer

      - name: Fetch Dependencies
        run: flutter pub get

      - name: Compile Release APK
        run: flutter build apk --release --no-tree-shake-icons

      - name: Upload APK Package
        uses: actions/upload-artifact@v4
        with:
          name: safargo-customer-apk
          path: build/app/outputs/flutter-apk/app-release.apk
          
